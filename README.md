# Camp-ChatGPT  — Sprint 1 Prototype

This repo is a deployable prototype aligned to Sprint 1 scope:
- Parent: register/login, add child, enroll child, view enrollment status, view schedule.
- Admin: create parents, create campers, create groups, assign campers to groups, create events, view/filter event list.

## Quick start (Docker Compose)
From the folder containing `docker-compose.yml`:

```bash
docker compose up --build 
```
http://localhost

